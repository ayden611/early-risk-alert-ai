.DS_Store
cardio_train.csv
instance/local.db

# ERA local validation exports — keep row-level MIMIC-derived files local unless approved
data/validation/latest_era_validation_export.csv
data/validation/local_exports/

# MIMIC / PhysioNet restricted-data safety
data/validation/local_private/
data/retro/
mimic_event_maps/
*.mimic.csv
*mimic*row*.csv

# ERA local-only restricted datasets / validation working files
data/eicu/
*.crdownload
*.csv.gz
# ERA local-only restricted dataset workspace
data/hirid/
*.parquet
*.zip
*.tar.gz
*.h5
*.hdf5
*.feather
*.pkl
*.pickle
# Local/private restricted validation data — never commit

# ERA local-only restricted validation data — never commit
hirid/raw/
*.csv
*.sqlite
*.sqlite-wal
*.sqlite-shm

# ERA_PRIVATE_INSURANCE_RECORDS_START
# Private insurance records must remain local only.
docs/private/insurance/
# ERA_PRIVATE_INSURANCE_RECORDS_END

# Local-only outreach and workflow-feedback records
docs/private/outreach/

# Private FDA regulatory records — local only
docs/private/regulatory/

# --- ERA local-only / private working artifacts (added 2026-08-07) ---
era_evidence_reconciliation/
era_archive_screening/
era/local_only/
tests/local_only/
grants/
ERA_Asset_Value_Assessment_*.docx
