# Early Risk Alert AI
## Canonical Review Scorer Decision
### 2026-08-07

**Decision: ADOPTED**

Canonical implementation:

`era/core/scoring.py::score_review_row()`

Canonical scorer ID:

`era-review-score-v1`

Scorer specification version:

`1.0.0`

Behavioral source boundary:

`4b792cc745bca5db03e8dfcb58c62cb22c3bef79`

## Basis

The canonical implementation is a single-source extraction of the corrected
application retrospective scorer recorded at the behavioral boundary above.

Extraction parity was demonstrated across 10,005 deterministic test cases:

- 5 fixed cases
- 10,000 seeded randomized cases

A second parity execution after canonical metadata was added also passed all
10,005 cases.

A frozen golden input/output contract is maintained under:

`tests/fixtures/canonical_review_score_golden.csv`

with enforcement in:

`tests/test_canonical_review_score_golden.py`

and CI workflow:

`.github/workflows/canonical-scorer-contract.yml`

## Evidence consequence

Historical MIMIC performance generated by the stepped validation-local
scorer is not canonical product-engine performance.

Historical eICU performance generated through a separate scoring path is
also not transferred to this scorer.

Those historical artifacts remain preserved for provenance.

New MIMIC and eICU evidence must be generated through the canonical scorer
and written to immutable run-specific artifacts.

The re-runs are new evidence baselines, not confirmation exercises.
Historical values are not required or expected to reproduce.

## Command Center boundary

The current visible Command Center uses predefined synthetic demonstration
scores rather than dynamically executing the canonical scorer.

Before subsequent external Command Center demonstration, synthetic vital
sign observations must be passed through the canonical scoring module so
the demonstration reflects actual product scoring behavior.
