# App
PORT=8000
LOG_LEVEL=INFO
SECRET_KEY=change-me

# Model
MODEL_PATH=demo_model.pkl
MODEL_META_PATH=model_meta.json

# DB (local dev can use sqlite, prod should use postgres)
DATABASE_URL=sqlite:///local.db
